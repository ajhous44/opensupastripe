/**
 * Data access layer — use repositories in lib/repositories/ and Server Actions in app/actions/.
 */
export const MIGRATION_STATUS = {
  completed: true,
  architecture: 'Server Actions + Repositories',
} as const
